export enum ApiStatus {
  IDLE,
  SUCCESS,
  LOADING,
  FAILED,
}
export enum LngType {
  TURKISH = "TURKISH",
  ENGLISH = "ENGLISH",
}
export enum ProductStatusType {
  AVAILABLE = 0,
  DEPLOYED = 1,
  ORDERED = 2,
  OUT_OF_STOCK = 3,
  DAMAGED = 4,
}
export enum CategoryType {
  ASSET = 0,
  LICENSE = 1,
  ACCESSORY = 2,
  CONSUMABLE = 3,
  COMPONENT = 4,
}
